{
  "name": "nomatic-events",
  "version": "0.1.1",
  "author": "Brian D. Foster <bdfoster89@gmail.com> (https://github.com/bdfoster)",
  "description": "Fast, asynchronous, and regex-enabled event framework",
  "main": "index.js",
  "scripts": {
    "test": "env NODE_ENV=test mocha --reporter spec --recursive test/**/*",
    "coverage": "istanbul cover --dir ./docs/coverage _mocha --recursive test/**/*",
    "codeclimate-test-reporter": "codeclimate-test-reporter < ./docs/coverage/lcov.info"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "codeclimate-test-reporter": "^0.3.3",
    "grunt": "^1.0.1",
    "grunt-browserify": "^5.0.0",
    "grunt-contrib-clean": "^1.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.0.2"
  },
  "dependencies": {
    "async": "^2.0.1",
    "lodash": "^4.15.0"
  }
}
