{
  "name": "nomatic-events",
  "version": "0.0.0",
  "author": "Brian D. Foster <bdfoster89@gmail.com> (https://github.com/bdfoster)",
  "description": "Fast, asynchronous, regex-enabled event framework for Node.js",
  "main": "index.js",
  "scripts": {
    "test": "env NODE_ENV=test mocha --reporter spec",
    "unit tests": "npm run test -- --recursive test/unit",
    "acceptance tests": "npm run test -- --recursive test/acceptance",
    "tests": "npm run test -- --recursive test/**/*",
    "coverage": "istanbul cover --dir ./docs/coverage _mocha --recursive test/**/*",
    "coveralls": "npm run cover -- --report lcovonly && cat ./docs/coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.2"
  },
  "dependencies": {
    "async": "^2.0.1",
    "lodash": "^4.15.0"
  }
}
